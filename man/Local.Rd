% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local.R
\name{Local}
\alias{Local}
\title{Local consensus tree}
\usage{
Local(trees, type = c("rooted", "induced"))
}
\arguments{
\item{trees}{A list of trees, or a \code{multiPhylo} object. All entries must
share the same leaf labels.}

\item{type}{Character specifying whether to compute the minimum rooted local
consensus (\code{"rooted"}, the default; MinRLC) or the minimum induced local
consensus (\code{"induced"}; MinILC).}
}
\value{
\code{Local()} returns an object of class \code{phylo} denoting the local
consensus tree. When there is no valid consensus (as no common triplets
separate any pair of trees), a star tree is returned.
}
\description{
\code{Local()} returns the local consensus
\insertCite{JanssonRajabySung2018}{ConsTree} of a set of rooted trees.
The local consensus is the most conservative tree consistent with the rooted
triplets shared by every input tree.  The minimum rooted local consensus
(MinRLC, \code{type = "rooted"}) and the minimum induced local consensus (MinILC,
\code{type = "induced"}) differ in how the resolution of the result is scored.
}
\details{
The implementation builds on the algorithms of
\insertCite{JanssonRajabySung2018}{ConsTree}; please cite that paper when
using this method.

Because the algorithm is exponential, \code{Local()} is limited to \code{n <= 20}
leaves.  Running time is faster when input trees are more congruent.
}
\examples{
# Two trees that agree on one cherry but disagree on overall topology
t1 <- ape::read.tree(text = "(1,((2,3),4));")
t2 <- ape::read.tree(text = "(1,((2,4),3));")
Local(list(t1, t2), "rooted")  # keeps clade {2,3,4} only

}
\references{
\insertAllCited{}
}
\seealso{
Closely related: \code{\link[=Strict]{Strict()}}, \code{\link[=Majority]{Majority()}}, \code{\link[=Adams]{Adams()}}.

Other consensus methods:
\code{\link[=Adams]{Adams()}},
\code{\link[=Average]{Average()}},
\code{\link[=Frequency]{Frequency()}},
\code{\link[=Greedy]{Greedy()}},
\code{\link[=Loose]{Loose()}},
\code{\link[=Majority]{Majority()}},
\code{\link[=MajorityPlus]{MajorityPlus()}},
\code{\link[=Quartet]{Quartet()}},
\code{\link[=RStar]{RStar()}},
\code{\link[=Strict]{Strict()}},
\code{\link[=Transfer]{Transfer()}}
}
\concept{consensus methods}
