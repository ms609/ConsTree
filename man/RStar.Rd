% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rstar.R
\name{RStar}
\alias{RStar}
\title{R* consensus tree}
\usage{
RStar(trees)
}
\arguments{
\item{trees}{A list of trees, or a \code{multiPhylo} object. All entries must
share the same leaf labels.}
}
\value{
\code{RStar()} returns the consensus tree, an object of class \code{phylo}.
It is rooted by construction, but when the resolved triplets leave the
deepest divergence unresolved the root is a polytomy.
}
\description{
\code{RStar()} returns the R* consensus \insertCite{Degnan2009}{ConsTree} of a
set of rooted trees.
}
\details{
The R* consensus is a rooted-triplet method.  For every set of three leaves it
tallies, across the input trees, the three possible resolved rooted triplets
(\code{ab|c}, \code{ac|b}, \code{bc|a}) and keeps whichever appears most frequently.
Ties are not kept.
The kept triplets form the set of majority resolved triplets, \eqn{R_{maj}}.
Then \eqn{R^*} is the most resolved tree that displays no resolved triplet
outside \eqn{R_{maj}}.

\eqn{R^*} is always a refinement of the majority-rule consensus:
every majority clade also appears in \code{RStar()}.

\code{RStar()} finds the strong clusters of \eqn{R_{maj}} among candidate clusters
built from triplet similarities \insertCite{Jansson2013,Jansson2016a}{ConsTree}.
For two trees it takes \eqn{O(n^2)} time \insertCite{Jansson2016a}{ConsTree},
testing each candidate with the criterion of
\insertCite{Jansson2013;textual}{ConsTree}.
For more trees it tallies every triplet directly
\insertCite{Bryant2003}{ConsTree}, in \eqn{O(kn^3)} time;
this outpaces the \eqn{O(n^2 \log^{k+2} n)}{O(n^2 log^(k+2) n)} algorithm of
\insertCite{Jansson2016a;textual}{ConsTree} unless \eqn{k} is very small.
Set \code{options(ConsTree.threads = )} to count triplets on several threads.
}
\examples{
# Five trees whose majority signal recovers the species tree (((a,b),c),d):
trees <- c(
  ape::read.tree(text = "(((a, b), c), d);"),
  ape::read.tree(text = "(((a, b), c), d);"),
  ape::read.tree(text = "(((a, b), c), d);"),
  ape::read.tree(text = "(((a, c), b), d);"),
  ape::read.tree(text = "(((b, c), a), d);")
)

# (a, b) wins {a,b,c} by plurality (3 vs 1 vs 1)
ape::write.tree(RStar(trees))

}
\references{
\insertAllCited{}
}
\seealso{
Closely related: \code{\link[=Strict]{Strict()}}, \code{\link[=Majority]{Majority()}}, \code{\link[=Adams]{Adams()}},
\code{\link[=Local]{Local()}}.

Other consensus methods:
\code{\link[=Adams]{Adams()}},
\code{\link[=Average]{Average()}},
\code{\link[=Frequency]{Frequency()}},
\code{\link[=Greedy]{Greedy()}},
\code{\link[=Local]{Local()}},
\code{\link[=Loose]{Loose()}},
\code{\link[=Majority]{Majority()}},
\code{\link[=MajorityPlus]{MajorityPlus()}},
\code{\link[=Quartet]{Quartet()}},
\code{\link[=Strict]{Strict()}},
\code{\link[=Transfer]{Transfer()}}
}
\concept{consensus methods}
