% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Quartet.R
\name{Quartet}
\alias{Quartet}
\title{Consensus tree that minimizes quartet distance}
\usage{
Quartet(
  trees,
  init = c("majority", "star", "extended"),
  greedy = c("best", "first")
)
}
\arguments{
\item{trees}{Object of class \code{multiPhylo} specifying the input trees.
All trees must share the same tip labels.}

\item{init}{Character string specifying the initial tree:
\itemize{
\item \code{"majority"}: the majority-rule consensus.
\item \code{"star"}: a fully unresolved star tree.
\item \code{"extended"}: the extended (greedy) majority-rule consensus.
}}

\item{greedy}{Character string specifying the greedy strategy:
\itemize{
\item \code{"best"}: evaluate all candidates and pick the best action at each step.
\item \code{"first"}: pick the first improving action encountered (faster; may give
a slightly worse result).
}}
}
\value{
\code{Quartet()} returns a consensus tree, an object of class \code{phylo},
unrooted.
}
\description{
\code{Quartet()} constructs a consensus tree that minimizes the sum of symmetric
quartet distances to a set of input trees, using a greedy add-and-prune
heuristic.
}
\details{
Where the majority-rule consensus minimizes the sum of Robinson-Foulds
distances to the input trees, \code{Quartet()} finds an approximate median tree
under the symmetric quartet distance \insertCite{Takazawa2026}{ConsTree},
which counts both false-positive and false-negative resolved quartets
equally.

Because the quartet distance gives greater weight to deep branches (which
resolve more quartets), quartet consensus trees tend to be more resolved
than majority-rule trees, especially when phylogenetic signal is low.

The function supports trees with up to 100 tips.
}
\examples{
library("TreeTools", quietly = TRUE)

# Generate bootstrap-like trees
trees <- as.phylo(1:30, nTip = 8)

# Quartet consensus
qc <- Quartet(trees)
plot(qc)

# Compare resolution with majority-rule
mr <- UnrootTree(Consensus(trees, p = 0.5))
cat("Majority-rule splits:", NSplits(mr), "\n")
cat("Quartet consensus splits:", NSplits(qc), "\n")

}
\references{
\insertAllCited{}
}
\seealso{
Closely related: \code{\link[=Strict]{Strict()}}, \code{\link[=Majority]{Majority()}}, \code{\link[=Greedy]{Greedy()}}.

Other consensus methods:
\code{\link[=Adams]{Adams()}},
\code{\link[=Average]{Average()}},
\code{\link[=Frequency]{Frequency()}},
\code{\link[=Greedy]{Greedy()}},
\code{\link[=Local]{Local()}},
\code{\link[=Loose]{Loose()}},
\code{\link[=Majority]{Majority()}},
\code{\link[=MajorityPlus]{MajorityPlus()}},
\code{\link[=RStar]{RStar()}},
\code{\link[=Strict]{Strict()}},
\code{\link[=Transfer]{Transfer()}}
}
\concept{consensus methods}
